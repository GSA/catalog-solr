version: '3'
services:
  solr:
    build:
      context: .
    image: catalog-solr
    ports:
      - "8983:8983"
  test:
    image: catalog-solr
    command: curl --silent --fail --location solr:8983/solr
    depends_on:
      - solr
